extends ../layouts/application.pug

block content
  h1= t('views.labels.new.creationLabel')
    +formFor()(action=route('labels') method='post')
      +input(label, "name")(type="text", value=t('views.labels.new.value'))
      +submit(label)(value=t('views.labels.new.button'))