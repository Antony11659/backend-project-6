extends ../layouts/application.pug

block content
  h1= t('views.statuses.new.creationStatus')
    +formFor()(action=route('statuses') method='post')
      +input(status, "name")(type="text", value=t('views.statuses.new.value'))
      +submit(status)(value=t('views.statuses.new.button'))